{% extends 'HSDOnSecBundle::base.html.twig' %}

{% block content %}
    <h1>Rollen</h1>

    <table>
        <tbody>
            {#<tr>#}
                {#<th>Id</th>#}
                {#<td>{{ role.id }}</td>#}
            {#</tr>#}
            <tr>
                <th>Name</th>
                <td>{{ role.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('role_index') }}">Zurück zur Liste</a>
        </li>
        <li>
            <a href="{{ path('role_edit', { 'id': role.id }) }}"><i class="fa fa-pencil" aria-hidden="true"></i></a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Löschen">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
