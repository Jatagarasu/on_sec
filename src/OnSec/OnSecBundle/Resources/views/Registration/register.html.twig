{% extends 'base.html.twig' %}
{% block content %}
    {% if message == 'success' %}
        <div class="row">
            <div class="col-md-12">
                <div class="alert alert-success" role="alert">
                    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                    <h4 class="alert-heading">Erfolgreich registriert</h4>
                    <p>Sie haben sich erfolgreich auf - OnSec - registriert und können sich ab sofort <a href="{{ path('login') }}">hier</a> einloggen</p>
                </div>
            </div>
        </div>
    {% endif %}
    {% if message == 'duplicateEntry' %}
        <div class="row">
            <div class="col-md-12">
                <div class="alert alert-warning" role="alert">
                    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                    <h4 class="alert-heading">Registrierung fehlgeschlagen</h4>
                    <p>Ihre Registrierung ist leider fehlgeschlagen. Ein benutzer mit dieser eMail Adresse existiert bereits. Wenn Sie sich bereits registriert haben können Sie sich <a href="{{ path('login') }}">hier</a> einloggen.</p>
                </div>
            </div>
        </div>
    {% endif %}
    <div class="row">
        <div class="col-md-12">
            {{ form_start(registerForm, { 'attr': {'class': 'form-control'} }) }}
            {{ form_row(registerForm.firstname) }}
            {{ form_row(registerForm.surname) }}
            {{ form_row(registerForm.email) }}
            {{ form_row(registerForm.plainPassword.first) }}
            {{ form_row(registerForm.plainPassword.second) }}
            {#{ form_row(registerForm.notificationActive) }#}

            <button class="btn btn-outline-primary" type="submit">Registrieren</button>
            {{ form_end(registerForm) }}
        </div>
    </div>
{% endblock %}