{% extends 'HSDOnSecBundle::base.html.twig' %}

{% block content %}
    <h1>Schlagwort anzeigen</h1>

    <table>
        <tbody>
            {#<tr>#}
                {#<th>Id</th>#}
                {#<td>{{ keyword.id }}</td>#}
            {#</tr>#}
            <tr>
                <th>Beschreibung</th>
                <td>{{ keyword.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('keyword_index') }}">Zurück zur Liste</a>
        </li>
        <li>
            <a href="{{ path('keyword_edit', { 'id': keyword.id }) }}"><i class="fa fa-pencil" aria-hidden="true"></i></a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Löschen">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
