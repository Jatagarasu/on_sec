{% extends 'HSDOnSecBundle::base.html.twig' %}
{% set page_title = "Nutzer anlegen" %}
{% block title %}{{page_title}}{% endblock %}

{% block content %}
    <h1>{{page_title}}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Speichern" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('user_index') }}">Zurück zur Liste</a>
        </li>
    </ul>
{% endblock %}
