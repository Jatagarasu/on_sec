{% extends 'HSDOnSecBundle::base.html.twig' %}

{% block content %}
    <h1>Raum anzeigen</h1>

    <table>
        <tbody>
            {#<tr>#}
                {#<th>Id</th>#}
                {#<td>{{ room.id }}</td>#}
            {#</tr>#}
            <tr>
                <th>Beschreibung</th>
                <td>{{ room.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('room_index') }}">Zurück zur Liste</a>
        </li>
        <li>
            <a href="{{ path('room_edit', { 'id': room.id }) }}"><i class="fa fa-pencil" aria-hidden="true"></i></a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Löschen">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
