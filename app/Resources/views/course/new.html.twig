{% extends 'base.html.twig' %}

{% block content %}
    <h1>Kurs erstellen</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('course_index') }}">Zurück zu der Liste</a>
        </li>
    </ul>
{% endblock %}
