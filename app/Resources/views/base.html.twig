<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8"/>
    <title>{% block title %}Welcome!{% endblock %}</title>
    {% block stylesheets %}
        {% stylesheets
        'css/bootstrap.min.css'
        'css/dashboard.css'
        'css/font-awesome.min.css'
        'css/main.css'
        'css/jquery-ui.min.css'
        'css/jquery-ui.structure.min.css'
        'css/jquery-ui.theme.min.css'
        'css/Instruction.css'
        'css/Course.css'
        'css/Style.css'
        filter='cssrewrite' output='css/compiled/app.css' %}
        <link rel="stylesheet" href="{{ asset_url }}" />
        {% endstylesheets %}
    {% endblock %}
    {% block ownStylesheets %} {% endblock %}
    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}"/>

    {% block javascript %}
        {% javascripts
        'js/angular.min.js'
        'js/tether.min.js'
        'js/jquery.min.js'
        'js/jquery-ui.min.js'
        'js/bootstrap.min.js'
        output='js/compiled/app.js' %}
        <script src="{{ asset_url }}"></script>
        {% endjavascripts %}
    {% endblock %}
</head>

{# Redirect to Login if user is not logged in #}
{% if not app.user and not ('login' in app.request.getRequestUri()) %}
    <meta http-equiv="refresh" content="1; url={{ path('login') }}">
    <script type="text/javascript">
        window.location.href = "{{ path('login') }}";
    </script>
{% else %}

    <body id="site-body">
    <header id="row">
        {% block header %}
            <div id="col-md-12">
                <div class="navbar navbar-mainmain">
                    {% block navigation %}
                        {% include 'navigation.html.twig' %}
                    {% endblock %}
                    <div class="col-md-3" style="text-align: right">
                        <h2>{{ '%s %s'|format(app.user.firstname,app.user.surname) }}<a style="color: #de021a;" href="{{ path('logout') }}"><i style="margin-top:5px;margin-left: 5px;" class="fa fa-sign-out" aria-hidden="true"></i></a></h2>
                    </div>
                </div>
            </div>
        {% endblock %}
    </header>
    <div class="container site-content">
        {% block content %}{% endblock %}
    </div>
    {% block footer %}
        <footer class="impressum" id="col-md-12">
            <a href class="footerSize text-white" data-toggle="modal" data-target="#myModal">Impressum und Copyright</a>

            <!-- Modal -->
            <div class="modal fade" id="myModal" role="dialog">
                <div class="modal-dialog">

                    <!-- Modal content-->
                    <div class="modal-content text-black">
                        <div class="modal-header">
                            <button type="button" class="close" data-dismiss="modal">&times;</button>
                            <h4 class="modal-title">Impressum und Copyright</h4>
                        </div>
                        <div class="modal-body">
                            <p>Hochschulprojekt "onSec" der Hochschule Düsseldorf</p>
                            <p>Projektbetreuung: Prof. Günther Witte und Lars-Arne Emmerich</p>
                            <p>beteiligte Studenten: Markus Holl, David Landry, Alexander Pleß, Tobias Quessel, Caroline Schneider und Daniel Vogelskamp</p>
                            siehe auch Impressum von <a href="https://www.hs-duesseldorf.de/impressum">Hochschule Düsseldorf</a>
                        </div>
                        <div class="modal-footer">
                            <p>©Hochschule Düsseldorf, 2017, all rights reserved</p>
                        </div>
                    </div>

                </div>
            </div>
        </footer>
    {% endblock %}
    </body>
{% endif %}
</html>

